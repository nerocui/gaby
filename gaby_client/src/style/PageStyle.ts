import {createUseStyles} from 'react-jss';

const useStyle = createUseStyles({
    root: {
        height: '100vh',
        width: '100vw',
        overflow: 'auto',
    },
});

export default useStyle;
